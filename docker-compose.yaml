services:
  songbird:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: songbird
    restart: unless-stopped
    ports:
      - "${SERVER_PORT:-5174}:${SERVER_PORT:-5174}"
    env_file:
      - path: .env
        required: false
    environment:
      APP_ENV: ${APP_ENV:-production}
      SERVER_PORT: ${SERVER_PORT:-5174}
    volumes:
      - songbird-data:/app/data

volumes:
  songbird-data:
